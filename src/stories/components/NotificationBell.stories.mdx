import { Meta, Story, Props, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import NotificationBell from '../../components/NotificationBell.vue';

<Meta
	title="Componentes/NotificationBell"
	component={ NotificationBell }
	argTypes={{
		variant: {
			control:{
				type: 'select',
				options: [
					'blue',
					'green',
					'purple',
					'orange',
					'gray'
				],
			}
		},
	}
}
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { NotificationBell },
	template: /*html*/ `
		<notification-bell
			v-bind="$props" 
		>
			notification-bell
		</notification-bell>`,
});

export const template = /*html*/ `
	<lc-notification-bell
		:isActive="true"
	>
		notification-bell
	</lc-notification-bell>`;

export const docsDecorator = () => {
	return {
		data() {
			return {
				template,
			};
		},
		template: /*html*/ `
			<source-code-wrapper
				:exampleSourceCode="template"
			>
				<story slot="component-preview"/>
			</source-code-wrapper>`,
	};
};

# NotificationBell
---

## Preview

<Canvas>
	<Story
		name="NotificationBell"
		decorators={[docsDecorator]}
		args={{
			isActive: true,
		}}
	>
		{ Template.bind({}) }
	</Story>
</Canvas>

<ArgsTable story="NotificationBell" />